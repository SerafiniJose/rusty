<?xml version="1.0" encoding="utf-8"?>
<!-- One downloadable Piper voice: name, what it costs and under what terms, and a single small
     icon action (download it, or remove a downloaded one). The voice's full dataset attribution
     is shown on the confirm card, where the user actually decides to install it. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingEnd="10dp">

        <TextView
            android:id="@+id/tvVoiceName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="@font/hanken_regular"
            android:maxLines="1"
            android:textColor="@color/ink"
            android:textSize="13sp" />

        <TextView
            android:id="@+id/tvVoiceMeta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="@font/hanken_regular"
            android:maxLines="1"
            android:textColor="@color/muted_dim"
            android:textSize="11sp" />
    </LinearLayout>

    <ImageButton
        android:id="@+id/btnVoiceAction"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/bg_tv_focus"
        android:padding="8dp"
        android:scaleType="fitCenter" />
</LinearLayout>
