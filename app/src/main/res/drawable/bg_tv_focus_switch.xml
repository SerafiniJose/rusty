<?xml version="1.0" encoding="utf-8"?>
<!-- D-pad focus ring for the settings switches: stroke-only, fully rounded into a pill so it
     outlines the switch rather than boxing it in a loose rectangle. -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- A view keeps isFocused() while its window loses focus to a dialog, so without this the
         ring behind an open card stays lit and the screen shows two focus rings at once. -->
    <item android:state_window_focused="false">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <corners android:radius="22dp" />
            <stroke
                android:width="2dp"
                android:color="@color/accent_fallback" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>
</selector>
