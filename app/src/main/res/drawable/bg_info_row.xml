<?xml version="1.0" encoding="utf-8"?>
<!-- Background for the Services & status rows: a transparent, hairline-bordered card so the rows
     read as one grouped list on the popup card. Wrapped in a ripple so touch feedback survives
     (the D-pad focus ring is drawn separately as android:foreground="@drawable/bg_tv_focus"). -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?android:attr/colorControlHighlight">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="@dimen/settings_card_radius" />
            <stroke
                android:width="1dp"
                android:color="@color/surface_border" />
        </shape>
    </item>
</ripple>
